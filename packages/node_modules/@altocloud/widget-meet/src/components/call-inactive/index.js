import React from 'react';
import classNames from 'classnames';

import SparkLogo from '@ciscospark/react-component-spark-logo';

import styles from './styles.css';

export default function InactiveCall() {
  return (
    <div className={classNames(`ciscospark-loading`, styles.loading)}>
      <div className={classNames(`ciscospark-loading`, styles.logo)}>
        <SparkLogo />
      </div>
      {`Altocloud Video Messaging Powered By Cisco Spark`}
    </div>
  );
}
