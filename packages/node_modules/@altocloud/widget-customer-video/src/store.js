import {
  applyMiddleware,
  compose,
  createStore
} from 'redux';

import thunk from 'redux-thunk';

import messageMeetReducers from './reducer';

export default createStore(
  messageMeetReducers,
  compose(applyMiddleware(thunk))
);
