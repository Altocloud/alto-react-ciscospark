import {combineReducers} from 'redux';
import {reducers as customerVideoReducers} from '@altocloud/widget-customer-video';
import guest from '@altocloud/react-redux-guest';

export const STATE_NAME = `AltocloudVideo`;

export const reducers = {guest, ...customerVideoReducers};
export default combineReducers(reducers);
